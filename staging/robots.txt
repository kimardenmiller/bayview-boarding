# https://www.robotstxt.org/robotstxt.html
#
# Now genuinely honored at its true location (Sept 25, 2026) - the site
# moved to its own custom domain, bayviewboarding.com, so this file is
# actually served at the domain root crawlers check by spec
# (bayviewboarding.com/robots.txt). Before that (Sept 19, 2026 note,
# no longer applicable) the site lived at a subpath of the shared
# kimardenmiller.github.io domain, so this copy was never actually
# discovered/honored - kept anyway back then for convention and the
# Sitemap reference, submittable to Search Console directly regardless.
User-agent: *
Disallow:

Sitemap: https://bayviewboarding.com/sitemap.xml
